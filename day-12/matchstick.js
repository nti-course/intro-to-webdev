/**
 *
 * @param {number} n - the number of houses
 * @returns {number} - the number of matchsticks needed to build the houses
 */
function matchstickHouse(n) {
  // write your code here
  return 5 * n + 1;
}

console.log(matchstickHouse(1));
console.log(matchstickHouse(2));
console.log(matchstickHouse(3));
